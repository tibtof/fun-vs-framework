group = "fun.vs.fw"
version = "0.0.1-SNAPSHOT"

// Root project acts as an aggregator only; subprojects define their own plugins and dependencies.
allprojects {
    repositories {
        mavenCentral()
    }
}
